# 轨迹质量筛选系统 - 使用说明

## 📋 概述

在轨迹蒸馏生成推理轨迹后，本系统通过LLM自动评估每条轨迹的质量，筛选出适合用于监督微调(SFT)的高质量数据。

## 🎯 筛选目标

### 1. **推理轨迹合理性评估**
- 每一跳的查询是否合理？
- 检索到的知识块是否相关？
- 推理过程是否呈现递进关系？
- 是否存在无效检索或重复检索？

### 2. **答案质量评估**
- 最终答案是否与标准答案一致？
- 答案是否完整回答了问题？
- 答案是否具有通用性？
- 答案是否符合专业标准？

## 🔍 评估维度 (9个核心维度)

| 维度 | 说明 | 一票否决项 |
|-----|------|-----------|
| 1. 问题通用性 | 问题是否通用、不特指论文、无多个子问题 | ✅ 是 |
| 2. 回答相关性 | 答案是否精准聚焦问题核心 | ❌ 否 |
| 3. 逻辑一致性 | 推理过程是否清晰连贯 | ❌ 否 |
| 4. 术语使用 | 专业术语是否准确 | ❌ 否 |
| 5. 事实正确性 | 技术细节是否符合事实 | ✅ 是 |
| 6. **推理轨迹合理性** | 轨迹是否合理有效 | ✅ 是 |
| 7. **答案一致性** | 答案是否与标准答案一致 | ✅ 是 |
| 8. 答案通用性 | 答案是否具有通用性 | ✅ 是 |
| 9. 答案完整性 | 答案是否准确完整 | ✅ 是 |

## 📊 质量评分标准

### ⭐ High (高质量)
- **适合直接用于SFT训练**
- 9个维度全部通过
- 无明显错误
- 推理轨迹合理
- 答案与标准答案基本一致

### 🔶 Medium (中等质量)
- **需修改后考虑使用**
- 存在轻微问题
- 推理轨迹有小瑕疵
- 答案与标准答案略有差异

### 🔴 Low (低质量)
- **不适合用于SFT**
- 存在严重缺陷
- 推理轨迹严重不合理
- 答案与标准答案严重不符
- 触发任何一票否决项

## 🏃 使用流程

### 步骤1: 准备输入数据

确保已运行 `轨迹蒸馏.py`，生成 `inference_trajectory.jsonl`

```bash
# 输入文件格式
{
  "question_id": "qa_001",
  "question": "什么是OLED显示技术？",
  "ground_truth_answer": "OLED是有机发光二极管",
  "final_model_answer": "OLED(有机发光二极管)是...",
  "full_trace": [
    {
      "hop": 1,
      "knowledge_gap": true,
      "reasoning": "需要检索OLED的基本原理",
      "query": "OLED显示技术的基本原理是什么？",
      "retrieved_chunks": ["...", "...", "..."]
    },
    ...
  ]
}
```

### 步骤2: 配置参数

编辑 `轨迹质量筛选.py` 中的 `CONFIG`:

```python
CONFIG = {
    # vLLM API配置
    "VLLM_API_ENDPOINT": "http://127.0.0.1:8000/v1/chat/completions",
    "VLLM_MODEL_NAME": "/path/to/your/model",
    
    # 输入文件
    "INPUT_FILE": "inference_trajectory.jsonl",
    
    # 输出文件
    "OUTPUT_HIGH_QUALITY": "high_quality_trajectory.jsonl",
    "OUTPUT_MEDIUM_QUALITY": "medium_quality_trajectory.jsonl",
    "OUTPUT_LOW_QUALITY": "low_quality_trajectory.jsonl",
    "OUTPUT_FILTERED": "filtered_trajectory.jsonl",  # 仅包含high
    
    # 并发控制
    "MAX_WORKERS": 4,
}
```

### 步骤3: 运行筛选

```bash
python3 轨迹质量筛选.py
```

### 步骤4: 查看结果

```bash
# 查看统计信息
cat << 'EOF'
评估完成，统计结果:
  高质量 (high):   45 条 (45.0%)
  中等质量 (medium): 30 条 (30.0%)
  低质量 (low):    20 条 (20.0%)
  评估失败:        5 条 (5.0%)
EOF

# 使用高质量数据
ls -lh filtered_trajectory.jsonl
```

## 📁 输出文件说明

| 文件 | 内容 | 用途 |
|-----|------|------|
| `high_quality_trajectory.jsonl` | 高质量数据 + 评估详情 | 查看详细评估报告 |
| `medium_quality_trajectory.jsonl` | 中等质量数据 + 改进建议 | 人工修改后使用 |
| `low_quality_trajectory.jsonl` | 低质量数据 + 问题分析 | 分析问题，改进蒸馏流程 |
| `filtered_trajectory.jsonl` | **仅高质量原始数据** | **直接用于训练** |

## 📝 输出格式示例

### High Quality 输出
```json
{
  "question_id": "qa_001",
  "evaluation_status": "success",
  "overall_rating": "high",
  "evaluation_result": {
    "quality_rating": {
      "overall": "high",
      "detailed_scores": {
        "question_universality": {"score": "high", "issues": []},
        "relevance": {"score": "high", "issues": []},
        "logical_consistency": {"score": "high", "issues": []},
        "terminology_usage": {"score": "high", "issues": []},
        "factual_correctness": {"score": "high", "issues": []},
        "reasoning_trajectory_validity": {"score": "high", "issues": []},
        "answer_consistency": {"score": "high", "issues": []},
        "answer_universality": {"score": "high", "issues": []},
        "answer_completeness": {"score": "high", "issues": []}
      }
    },
    "improvement_suggestions": ["可以增加更多技术细节"]
  },
  "original_data": { ... }  // 原始轨迹数据
}
```

### Medium Quality 输出
```json
{
  "question_id": "qa_002",
  "evaluation_status": "success",
  "overall_rating": "medium",
  "evaluation_result": {
    "quality_rating": {
      "overall": "medium",
      "detailed_scores": {
        "question_universality": {"score": "high", "issues": []},
        "relevance": {"score": "medium", "issues": ["答案略显简略，未涵盖所有关键点"]},
        "reasoning_trajectory_validity": {"score": "medium", "issues": ["第2跳的查询与第1跳重复度较高"]},
        ...
      }
    },
    "improvement_suggestions": [
      "建议在答案中增加LCD和OLED的对比",
      "优化第2跳的查询，避免与第1跳重复"
    ]
  },
  "original_data": { ... }
}
```

### Low Quality 输出
```json
{
  "question_id": "qa_003",
  "evaluation_status": "success",
  "overall_rating": "low",
  "evaluation_result": {
    "quality_rating": {
      "overall": "low",
      "detailed_scores": {
        "question_universality": {"score": "low", "issues": ["问题包含2个子问题：技术原理和应用场景"]},
        "answer_consistency": {"score": "low", "issues": ["答案与标准答案在核心技术参数上存在严重偏差"]},
        "reasoning_trajectory_validity": {"score": "low", "issues": ["第3跳检索到的知识块与问题无关"]},
        ...
      }
    },
    "improvement_suggestions": [
      "将问题拆分为两个独立的问题",
      "重新蒸馏，优化检索策略"
    ]
  },
  "original_data": { ... }
}
```

## 🔄 完整工作流

```
qa_data.jsonl
    ↓
[轨迹蒸馏.py]
    ↓
inference_trajectory.jsonl (100条)
    ↓
[轨迹质量筛选.py]  ← 本脚本
    ↓
    ├─→ high_quality_trajectory.jsonl (45条) ✅
    ├─→ medium_quality_trajectory.jsonl (30条) 🔶
    └─→ low_quality_trajectory.jsonl (25条) 🔴
    ↓
filtered_trajectory.jsonl (45条，仅包含high)
    ↓
[轨迹数据整理.py]
    ↓
train_agent_data.jsonl + eval_agent_data.jsonl
    ↓
用于SFT训练
```

## 🎓 典型场景

### 场景1: 大规模数据蒸馏 + 自动筛选
```bash
# 1. 蒸馏1000条QA
python3 轨迹蒸馏.py  # 生成 inference_trajectory.jsonl (1000条)

# 2. 自动质量筛选
python3 轨迹质量筛选.py  # 筛选出 ~450条高质量数据

# 3. 转换为训练格式
python3 轨迹数据整理.py  # 使用 filtered_trajectory.jsonl 作为输入
```

### 场景2: 迭代改进
```bash
# 1. 分析低质量数据
cat low_quality_trajectory.jsonl | jq '.evaluation_result.improvement_suggestions'

# 2. 根据问题改进蒸馏策略
#    - 优化Judge Prompt
#    - 调整RAG检索参数
#    - 改进翻译逻辑

# 3. 重新蒸馏
python3 轨迹蒸馏.py  # 使用改进的配置

# 4. 再次筛选
python3 轨迹质量筛选.py  # 验证改进效果
```

## ⚙️ 高级配置

### 调整评估严格度

在 `SYSTEM_PROMPT_EVALUATOR` 中修改评分标准：

```python
# 更严格
*   `high`：**无任何瑕疵**，9个维度全部完美...

# 更宽松
*   `high`：**无严重错误**，允许轻微表述不完美...
```

### 自定义维度权重

可以在评估prompt中增加权重说明：

```python
##维度权重：
- 推理轨迹合理性: 30%
- 答案一致性: 25%
- 答案完整性: 20%
- 其他维度: 25%
```

## 🐛 常见问题

### Q1: 评估速度太慢怎么办？
**A**: 增加并发数
```python
CONFIG = {
    "MAX_WORKERS": 8,  # 根据服务器性能调整
}
```

### Q2: High质量数据太少怎么办？
**A**: 
1. 检查 `medium_quality_trajectory.jsonl` 中的改进建议
2. 根据建议优化蒸馏脚本
3. 考虑适当放宽筛选标准

### Q3: 评估结果JSON解析失败？
**A**: 
1. 检查vLLM模型是否支持JSON输出
2. 在prompt中加强JSON格式要求
3. 增加后处理逻辑提取JSON

## 📊 质量提升建议

### 提升High比例的方法

1. **优化蒸馏Prompt**
   - 更明确的推理要求
   - 更详细的答案生成指导

2. **改进RAG检索**
   - 调整top_k参数
   - 优化rerank策略
   - 增强去重逻辑

3. **增加后处理**
   - 答案格式规范化
   - 术语标准化
   - 逻辑一致性检查

4. **使用更好的标注数据**
   - 提供高质量的ground_truth
   - 确保问题表述清晰
   - 避免歧义

## ✅ 总结

| 特性 | 说明 |
|-----|------|
| **输入** | inference_trajectory.jsonl (轨迹蒸馏输出) |
| **输出** | filtered_trajectory.jsonl (高质量数据) |
| **评估维度** | 9个核心维度 |
| **质量标准** | High / Medium / Low |
| **并发支持** | ✅ 支持 |
| **自动化程度** | 🤖 全自动 |

---

## 🚀 快速开始

```bash
# 1. 确保vLLM服务运行
curl http://127.0.0.1:8000/v1/models

# 2. 准备输入数据
ls inference_trajectory.jsonl

# 3. 运行筛选
python3 轨迹质量筛选.py

# 4. 使用筛选结果
cp filtered_trajectory.jsonl trajectory.jsonl
python3 轨迹数据整理.py
```
