# 搜索服务监控问题修复说明

## 问题诊断

### 症状
```
总请求数: 513
成功: 1 (只有1次成功！)
失败: 0
超时: 0  
活跃请求: 0
所有阶段调用次数: 0 (从未进入pipeline！)
缓存命中率: 0.2%
```

### 根本原因
**512个请求在参数校验阶段就被拦截了，根本没有执行搜索逻辑！**

调用速度很快的原因是：**请求在参数校验阶段（毫秒级）就返回了**，根本没有执行：
- 编码服务调用（秒级）
- 向量召回（秒级）
- 数据库查询（秒级）
- 重排序（秒级）

所以感觉"速度提升了几十倍"，实际上是**什么都没做就返回了**！

### 代码缺陷

原代码：
```python
@app.route('/api-rqa-search/search', methods=['POST'])
def get_data():
    req_id = int(time.time() * 1000) % 1000000
    
    try:
        form = request.form
        query = form.get('query', '', str)
        id = form.get('id', 0, int)
        top_doc_num = form.get('top_doc_num', 0, int)
        
        MONITOR.start_request(req_id, query)  # ✅ 记录了请求开始
        TIMEOUT_MANAGER.enter_request()
        
        # ❌ 问题：参数校验失败时直接return，没有更新监控！
        if not query:
            return json_result(-1, 'query must not be null.', None)  # ← 直接返回
        if not id:
            return json_result(-1, 'id must not be null.', None)      # ← 直接返回
        if not top_doc_num:
            return json_result(-1, 'top_doc_num must not be null.', None)  # ← 直接返回
        
        # 搜索逻辑...
        MONITOR.end_request(req_id, success=True)  # ← 只有成功时才被调用
    except:
        MONITOR.end_request(req_id, success=False)
```

**问题链：**
1. 请求进来 → `MONITOR.start_request()` → `total_requests++`
2. 参数校验失败 → 直接 `return` → **没有调用 `MONITOR.end_request()`**
3. 监控统计不完整 → 看起来有513个请求，但只有1个成功

### 可能的客户端问题

最可能的原因：**客户端使用了错误的参数传递方式**

```python
# ❌ 错误的调用（可能正在使用）
response = requests.post('http://10.70.223.31:9510/api-rqa-search/search',
                         json={'query': 'test', 'id': 123, 'top_doc_num': 5})
                         # ↑ 使用json，服务端用form解析，导致参数全部为空

# ✅ 正确的调用
response = requests.post('http://10.70.223.31:9510/api-rqa-search/search',
                         data={'query': 'test', 'id': 123, 'top_doc_num': 5})
                         # ↑ 使用data (form-encoded)
```

## 修复方案

### 方案1：修复监控代码（推荐）

使用 `try-finally` 确保监控总是被更新：

```python
@app.route('/api-rqa-search/search', methods=['POST'])
def get_data():
    req_id = int(time.time() * 1000) % 1000000
    success = False  # 默认失败
    
    MONITOR.start_request(req_id, '')
    TIMEOUT_MANAGER.enter_request()
    
    try:
        # 参数解析
        form = request.form
        query = form.get('query', '', str)
        id = form.get('id', 0, int)
        top_doc_num = form.get('top_doc_num', 0, int)
        
        # 参数校验（失败时直接return，但finally会更新监控）
        if not query:
            return json_result(-1, 'query must not be null.', None)
        if not id:
            return json_result(-1, 'id must not be null.', None)
        if not top_doc_num:
            return json_result(-1, 'top_doc_num must not be null.', None)
        
        # 搜索逻辑...
        # ...
        
        success = True  # 成功时设置
        return json_result(0, '', data)
        
    except Exception as e:
        return json_result(-1, str(e), None)
        
    finally:
        # ✅ 无论如何都会执行
        MONITOR.end_request(req_id, success=success)
        TIMEOUT_MANAGER.exit_request()
```

### 方案2：支持多种参数格式

同时支持 `form data` 和 `json`：

```python
# 优先使用form data，如果没有则尝试json
if request.form:
    query = request.form.get('query', '', str)
    id = request.form.get('id', 0, int)
    top_doc_num = request.form.get('top_doc_num', 0, int)
elif request.is_json:
    json_data = request.get_json()
    query = json_data.get('query', '')
    id = json_data.get('id', 0)
    top_doc_num = json_data.get('top_doc_num', 0)
else:
    query = ''
    id = 0
    top_doc_num = 0
```

### 方案3：添加详细日志

```python
# 参数校验失败时记录日志
if not query:
    print(f'[Request {req_id}] ⚠️  Rejected: empty query')
    return json_result(-1, 'query must not be null.', None)

# 参数校验成功时记录日志
print(f'[Request {req_id}] ✅ Validated: query="{query[:50]}", id={id}, top_doc_num={top_doc_num}')

# 完成时记录日志
print(f'[Request {req_id}] ✅ Completed: {len(results)} results in {duration:.2f}s')
```

## 验证方法

### 步骤1：运行诊断脚本

```bash
python test_client.py 10.70.223.31 9510
```

这个脚本会：
1. 测试不同的参数传递方式（data vs json）
2. 测试参数校验（缺少参数）
3. 对比测试前后的统计数据
4. 分析哪些请求成功，哪些失败

### 步骤2：检查统计数据

```bash
curl http://10.70.223.31:9510/api-rqa-search/stats
```

关注这些指标：
- `total_requests` vs `successful_requests + failed_requests + timeout_requests`
- 如果不相等，说明监控有问题
- `stage_times` 各阶段的调用次数
- 如果全是0，说明请求没有真正执行

### 步骤3：修改客户端代码

如果发现是参数传递问题：

```python
# 修改前（错误）
response = requests.post(url, json={...})

# 修改后（正确）
response = requests.post(url, data={...})
```

## 预期结果

修复后，监控数据应该显示：

```
总请求数: 513
成功: X  ← 实际成功的请求数
失败: Y  ← 参数校验失败的请求数（X + Y = 513）
超时: 0
阶段调用次数: > 0  ← 成功的请求会执行各阶段
```

## 文件清单

1. **PROBLEM_ANALYSIS.md** - 详细的问题分析报告
2. **test_client.py** - 客户端测试脚本
3. **diagnose_real_service.py** - 服务诊断脚本
4. **search_srv_FIXED.py** - 修复后的服务代码（关键部分）
5. **search_srv_diagnose.py** - 简化的诊断服务（用于测试监控逻辑）
6. **修复说明.md** - 本文件

## 快速修复

如果你需要立即修复，只需要在你的代码中做一个改动：

```python
@app.route('/api-rqa-search/search', methods=['POST'])
def get_data():
    req_id = int(time.time() * 1000) % 1000000
    success = False  # ← 添加这行
    
    MONITOR.start_request(req_id, query)
    TIMEOUT_MANAGER.enter_request()
    
    try:
        # 原有代码...
        
        success = True  # ← 成功时设置
        return json_result(code, msg, data)
        
    except Exception as e:
        # 原有异常处理...
        return json_result(-1, str(e), None)
        
    finally:  # ← 添加finally块
        MONITOR.end_request(req_id, success=success)
        TIMEOUT_MANAGER.exit_request()
```

这样就能确保所有请求都被正确记录，无论是成功、失败还是参数校验失败。
