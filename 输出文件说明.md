# 输出文件说明

## 📊 当前状态

### ✅ 已创建的文件

| 文件 | 类型 | 说明 |
|-----|------|------|
| `轨迹质量筛选.py` | 脚本 | 质量筛选主程序 |
| `inference_trajectory_sample.jsonl` | 示例输入 | 3条示例数据 |
| 各种.md文档 | 文档 | 使用说明 |

### ❌ 需要运行才能生成的文件

| 文件 | 何时生成 | 说明 |
|-----|---------|------|
| `filtered_trajectory.jsonl` | 运行筛选后 | **主要输出** - 高质量轨迹数据 |
| `high_quality_trajectory.jsonl` | 运行筛选后 | 高质量+评估报告 |
| `medium_quality_trajectory.jsonl` | 运行筛选后 | 中等质量+改进建议 |
| `low_quality_trajectory.jsonl` | 运行筛选后 | 低质量+问题分析 |

## 🚫 为什么没有输出文件？

因为 `轨迹质量筛选.py` 需要**vLLM服务**才能运行：

```bash
# 需要先启动vLLM
vllm serve /path/to/model --port 8000

# 然后才能运行
python3 轨迹质量筛选.py
```

当前vLLM服务未运行，所以无法生成输出文件。

## 💡 两种方式获取输出

### 方式1: 运行完整流程 (需要vLLM)

```bash
# 1. 启动vLLM服务
vllm serve /path/to/model --port 8000

# 2. 运行筛选（会自动生成所有输出文件）
python3 轨迹质量筛选.py
```

### 方式2: 查看模拟输出示例 (不需要vLLM)

我已经创建了模拟输出文件，展示输出格式：
- `filtered_trajectory_example.jsonl` - 筛选后的高质量数据示例
- `high_quality_example.jsonl` - 完整评估报告示例
- `medium_quality_example.jsonl` - 中等质量示例
- `low_quality_example.jsonl` - 低质量示例

## 📋 完整流程

```
输入: inference_trajectory.jsonl (来自轨迹蒸馏)
  ↓
运行: python3 轨迹质量筛选.py
  ↓
输出:
  ├─ filtered_trajectory.jsonl ⭐ (仅高质量数据)
  ├─ high_quality_trajectory.jsonl (高质量+评估)
  ├─ medium_quality_trajectory.jsonl (中等+建议)
  └─ low_quality_trajectory.jsonl (低质量+分析)
```

## ✅ 下一步

1. **如果有vLLM服务**: 运行 `python3 轨迹质量筛选.py`
2. **如果没有vLLM**: 查看下面的示例输出文件了解格式
