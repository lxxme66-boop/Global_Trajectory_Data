# 📦 完整文件清单和说明

## ✅ 已创建的文件

### 1️⃣ 核心脚本 (3个)

| 文件 | 大小 | 说明 | 运行命令 |
|-----|------|------|---------|
| `轨迹蒸馏.py` | 30KB | 生成推理轨迹 | `python3 轨迹蒸馏.py` |
| `轨迹质量筛选.py` | 18KB | **⭐ 新增** - 质量筛选 | `python3 轨迹质量筛选.py` |
| `轨迹数据整理.py` | 11KB | 转换为训练格式 | `python3 轨迹数据整理.py` |

### 2️⃣ 示例输出文件 (4个) - **⭐ 新创建**

这些是**模拟输出**，展示运行 `轨迹质量筛选.py` 后的文件格式：

| 文件 | 大小 | 内容 |
|-----|------|------|
| `filtered_trajectory_example.jsonl` | 1.8KB | ⭐ **高质量轨迹数据**（原始格式） |
| `high_quality_example.jsonl` | 2.7KB | 高质量+完整评估报告 |
| `medium_quality_example.jsonl` | 2.5KB | 中等质量+改进建议 |
| `low_quality_example.jsonl` | 2.9KB | 低质量+问题分析 |

### 3️⃣ 文档 (10个)

| 文件 | 大小 | 说明 |
|-----|------|------|
| **`新增功能总结.txt`** | 16KB | **⭐ 推荐首读** |
| `质量筛选使用说明.md` | 9.6KB | 详细使用指南 |
| `质量筛选快速参考.md` | 6.9KB | 快速查阅 |
| `完整流程说明.md` | 13KB | 三步流程 |
| `文件清单和说明.md` | - | 本文档 |
| `输出文件说明.md` | - | 输出文件解释 |
| `轨迹蒸馏输入说明.md` | 9.0KB | 蒸馏输入格式 |
| `问题答案总结.md` | 5.6KB | 问题答案 |
| `运行说明.md` | 8.4KB | 运行指南 |
| `API调用示例.md` | 6.9KB | API调用演示 |

### 4️⃣ 示例输入文件

| 文件 | 大小 | 说明 |
|-----|------|------|
| `inference_trajectory_sample.jsonl` | 3.4KB | 3条示例轨迹（用于测试筛选）|
| `qa_data.jsonl` | 345B | 2条QA示例 |
| `trajectory.jsonl` | 3.2KB | 2条轨迹示例 |

---

## ❌ 需要运行才会生成的文件

### 运行 `轨迹质量筛选.py` 后会生成：

| 文件 | 说明 | 对应示例文件 |
|-----|------|-------------|
| `filtered_trajectory.jsonl` | ⭐ **实际的高质量数据** | `filtered_trajectory_example.jsonl` |
| `high_quality_trajectory.jsonl` | 实际的高质量+评估 | `high_quality_example.jsonl` |
| `medium_quality_trajectory.jsonl` | 实际的中等质量+建议 | `medium_quality_example.jsonl` |
| `low_quality_trajectory.jsonl` | 实际的低质量+分析 | `low_quality_example.jsonl` |

⚠️ **重要**: 这些文件需要**vLLM服务运行**才能生成！

---

## 📊 示例输出文件内容展示

### 1. `filtered_trajectory_example.jsonl` (⭐ 最重要)

**格式**: 与输入相同的原始轨迹数据，但只包含高质量的

```bash
# 查看示例
head -n 1 /workspace/filtered_trajectory_example.jsonl | jq .
```

**输出**:
```json
{
  "question_id": "qa_001",
  "question": "什么是OLED显示技术？",
  "ground_truth_answer": "OLED是...",
  "final_model_answer": "OLED(有机发光二极管)是...",
  "full_trace": [...]
}
```

**用途**: 直接用于下一步处理 (`轨迹数据整理.py`)

---

### 2. `high_quality_example.jsonl`

**格式**: 原始数据 + 详细评估报告

```bash
# 查看评估结果
cat /workspace/high_quality_example.jsonl | jq '.evaluation_result.quality_rating.overall'
# 输出: "high"

# 查看详细评分
cat /workspace/high_quality_example.jsonl | jq '.evaluation_result.quality_rating.detailed_scores'
```

**输出**:
```json
{
  "question_universality": {"score": "high", "issues": []},
  "relevance": {"score": "high", "issues": []},
  "reasoning_trajectory_validity": {"score": "high", "issues": []},
  "answer_consistency": {"score": "high", "issues": []},
  ...全部9个维度
}
```

**用途**: 查看详细评估报告，了解为什么是高质量

---

### 3. `medium_quality_example.jsonl`

**格式**: 原始数据 + 评估 + **改进建议**

```bash
# 查看改进建议
cat /workspace/medium_quality_example.jsonl | jq '.evaluation_result.improvement_suggestions'
```

**输出**:
```json
[
  "优化第2跳的查询，避免与第1跳重复",
  "在答案中增加液晶分子旋转的详细机制",
  "补充TFT-LCD的层次结构"
]
```

**用途**: 了解如何改进，人工优化后可纳入训练集

---

### 4. `low_quality_example.jsonl`

**格式**: 原始数据 + 评估 + **问题详情**

```bash
# 查看具体问题
cat /workspace/low_quality_example.jsonl | jq '.evaluation_result.quality_rating.detailed_scores.reasoning_trajectory_validity'
```

**输出**:
```json
{
  "score": "low",
  "issues": [
    "第1跳的查询'OLED技术？'过于笼统",
    "检索到的知识块（量子点、MicroLED）与问题完全无关",
    "推理过程严重偏离主题"
  ]
}
```

**用途**: 分析问题模式，改进蒸馏脚本

---

## 🎯 如何使用示例文件

### 场景1: 了解输出格式

```bash
# 查看高质量数据的格式
cat filtered_trajectory_example.jsonl | jq . | head -n 50

# 查看评估报告的结构
cat high_quality_example.jsonl | jq '.evaluation_result' | head -n 30
```

### 场景2: 理解评分标准

```bash
# 对比三种质量等级
echo "=== High Quality ==="
cat high_quality_example.jsonl | jq '.evaluation_result.quality_rating.overall'

echo "=== Medium Quality ==="
cat medium_quality_example.jsonl | jq '.evaluation_result.quality_rating.overall'

echo "=== Low Quality ==="
cat low_quality_example.jsonl | jq '.evaluation_result.quality_rating.overall'
```

### 场景3: 学习改进方法

```bash
# 查看所有改进建议
cat medium_quality_example.jsonl | jq '.evaluation_result.improvement_suggestions[]'
cat low_quality_example.jsonl | jq '.evaluation_result.improvement_suggestions[]'
```

---

## 🚀 生成实际输出文件

### 前提条件

1. **vLLM服务已启动**
   ```bash
   curl http://127.0.0.1:8000/v1/models
   ```

2. **有实际的输入数据**
   ```bash
   ls inference_trajectory.jsonl  # 来自轨迹蒸馏
   ```

### 运行命令

```bash
# 运行质量筛选
python3 轨迹质量筛选.py

# 查看生成的文件
ls -lh filtered_trajectory.jsonl
ls -lh high_quality_trajectory.jsonl
ls -lh medium_quality_trajectory.jsonl
ls -lh low_quality_trajectory.jsonl
```

### 预期输出

```
评估完成，统计结果:
  高质量 (high):   450条 (45%)
  中等质量 (medium): 300条 (30%)
  低质量 (low):    250条 (25%)

已写入 450 条高质量轨迹到: filtered_trajectory.jsonl
```

---

## 📈 完整数据流

```
输入 (示例)
  └─ inference_trajectory_sample.jsonl (3条)

     ↓ [查看示例输出，了解格式]

示例输出
  ├─ filtered_trajectory_example.jsonl (1条高质量)
  ├─ high_quality_example.jsonl (1条+评估)
  ├─ medium_quality_example.jsonl (1条+建议)
  └─ low_quality_example.jsonl (1条+问题)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

实际运行 (有vLLM)
输入
  └─ inference_trajectory.jsonl (1000条)

     ↓ [python3 轨迹质量筛选.py]

实际输出
  ├─ filtered_trajectory.jsonl (450条高质量) ⭐
  ├─ high_quality_trajectory.jsonl (450条+评估)
  ├─ medium_quality_trajectory.jsonl (300条+建议)
  └─ low_quality_trajectory.jsonl (250条+问题)
```

---

## ✅ 总结

### 已创建 ✅
- **脚本**: 轨迹质量筛选.py (18KB)
- **示例输出**: 4个 example.jsonl 文件
- **文档**: 10个 .md/.txt 文档

### 需要运行才能生成 ⏳
- filtered_trajectory.jsonl (实际高质量数据)
- high/medium/low_quality_trajectory.jsonl (实际评估报告)

### 运行条件 🔧
- ✅ vLLM服务 (端口8000)
- ✅ inference_trajectory.jsonl (输入数据)

---

## 💡 快速查看示例

```bash
# 查看所有示例文件
ls -lh /workspace/*_example.jsonl

# 查看高质量示例的评分
cat /workspace/high_quality_example.jsonl | jq '.overall_rating'
# 输出: "high"

# 查看低质量示例的问题
cat /workspace/low_quality_example.jsonl | jq '.evaluation_result.quality_rating.detailed_scores.reasoning_trajectory_validity.issues'
```
