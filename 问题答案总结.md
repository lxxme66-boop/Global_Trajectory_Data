# 问题答案总结

## 🎯 问题1: 轨迹蒸馏.py的输入需要哪些字段？

### 简短回答
**必需2个字段，可选1个字段**：

| 字段 | 必需性 | 说明 |
|-----|-------|------|
| `question_id` | ✅ 必需 | 问题的唯一标识符 |
| `question` | ✅ 必需 | 用户的问题内容 |
| `answer` | ❌ 可选 | 标准答案(仅用于记录,不参与推理) |

### 完整示例
```json
{"question_id": "qa_001", "question": "什么是OLED显示技术？", "answer": "OLED是有机发光二极管"}
{"question_id": "qa_002", "question": "TFT-LCD的工作原理是什么？"}
```

### 验证逻辑
- ✅ 如果 `question_id` 和 `question` 都存在 → 正常处理
- ❌ 如果缺少任意一个 → **自动跳过该条数据**
- 代码位置：`轨迹蒸馏.py` 第414-420行

---

## 🤖 问题2: 用的是火山API吗，那vLLM是干啥的？

### 简短回答
**两者都用，但作用完全不同**：

- **vLLM = 大脑** (核心推理引擎，不可缺少)
- **火山API = 翻译助手** (辅助功能，可以禁用)

### 详细对比

| 对比项 | vLLM | 火山引擎API |
|-------|------|------------|
| **作用** | 推理决策(判断+生成答案) | 中英文翻译+语种检测 |
| **重要性** | 🔴 核心(100%) | 🟡 辅助(30%) |
| **能否禁用** | ❌ 不能(禁用后脚本无法运行) | ✅ 可以(降级为单语检索) |
| **调用次数** | 3-6次/问题 | 4-10次/问题 |
| **服务地址** | http://127.0.0.1:8000 | 火山云API |
| **模型** | Qwen3-32B(微调后) | 火山翻译引擎 |

---

## 🔍 vLLM的具体作用

### 作用1: Judge阶段 - 判断知识缺口
**调用时机**: 每一跳开始时

```python
# 输入
{
  "问题": "什么是OLED?",
  "已有上下文": "无" (或之前检索的知识)
}

# vLLM输出
{
  "knowledge_gap": true,  # 是否需要更多信息
  "reasoning": "需要检索OLED的基本原理",
  "query": "OLED显示技术的基本原理是什么？"  # 生成搜索查询
}
```

### 作用2: Answer阶段 - 生成最终答案
**调用时机**: 知识收集完成后

```python
# 输入
{
  "问题": "什么是OLED?",
  "上下文": "[知识块1] OLED是有机发光二极管...\n[知识块2] OLED不需要背光..."
}

# vLLM输出
"OLED(有机发光二极管)是一种显示技术，它使用有机材料在电流作用下发光..."
```

---

## 🌐 火山API的具体作用

### 唯一用途: 双语混合检索

```
原始查询(中文): "OLED显示技术的基本原理是什么？"
    ↓
火山API语种检测 → "zh"
    ↓
火山API翻译 → "What is the basic principle of OLED display technology?"
    ↓
并行RAG检索:
  - 用中文查询 → 检索中文文档
  - 用英文查询 → 检索英文文档
    ↓
合并去重 → 提高召回率(找到更多相关文档)
```

**为什么需要翻译？**
- 知识库可能同时包含中英文文档
- 单语检索可能遗漏另一种语言的文档
- 双语检索提高知识覆盖率

---

## 📊 完整工作流程

```
用户问题
  ↓
┌──────────────────────────────────┐
│ Hop 1                             │
│  1. vLLM判断 → 需要更多信息       │ ← vLLM (核心)
│  2. 火山翻译 → 中→英              │ ← 火山API (辅助)
│  3. RAG检索 → 返回知识块          │
└──────────────────────────────────┘
  ↓ (累积上下文)
┌──────────────────────────────────┐
│ Hop 2                             │
│  (重复上述流程)                   │
└──────────────────────────────────┘
  ↓
┌──────────────────────────────────┐
│ Hop N                             │
│  vLLM判断 → 信息足够 (停止)       │ ← vLLM (核心)
└──────────────────────────────────┘
  ↓
┌──────────────────────────────────┐
│ vLLM生成最终答案                  │ ← vLLM (核心)
└──────────────────────────────────┘
```

---

## 🔑 核心要点

### 1. vLLM是主角
- 🧠 负责所有的**思考**和**决策**
- 📝 负责**生成答案**
- ❌ **缺少vLLM，脚本完全无法运行**

### 2. 火山API是配角
- 🌐 只负责**翻译**
- 🎯 目的是提高RAG检索的**召回率**
- ✅ **没有火山API，脚本仍可运行**(单语模式)

### 3. 类比理解
```
vLLM = 侦探的大脑
  - 判断线索是否充足
  - 决定下一步调查方向
  - 整合所有信息得出结论

火山API = 侦探的翻译助手
  - 帮助侦探阅读外文线索
  - 提高获取线索的范围
  - 有它更好，没它也能工作(只看中文线索)
```

---

## ✅ 总结表

| 问题 | 核心答案 |
|-----|---------|
| **输入必需字段** | `question_id` ✅ + `question` ✅ + `answer` ❌(可选) |
| **vLLM的作用** | **核心大脑** - 判断知识缺口 + 生成答案 (不可缺少) |
| **火山API的作用** | **翻译助手** - 中英互译提升RAG召回率 (可以禁用) |
| **谁更重要？** | **vLLM >> 火山API** (100% vs 30%) |

---

## 📚 相关文档

- `轨迹蒸馏输入说明.md` - 输入格式和API详细说明
- `API调用示例.md` - 完整的调用时间线演示
- `快速参考.txt` - 快速查阅手册
- `运行说明.md` - 完整运行指南
