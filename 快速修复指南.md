# MongoDB 超时问题 - 快速修复指南

## ⚡ 立即修复（1 分钟）

### 1. 停止当前服务
```bash
./stop_search_cluster.sh
```

### 2. 使用修复后的代码
```bash
# 修复后的代码已更新到：
/workspace/search_srv_pipeline_v3_turbo.py
```

### 3. 重启服务
```bash
./start_search_cluster.sh
```

### 4. 验证修复
```bash
# 查看日志
tail -f logs/search_9511.log

# 期望看到：
# [MongoDB Batch 1/X] 🔄 Querying 200-300 conditions...
# [MongoDB Batch 1/X] ✅ XXX records in XX.XXs
```

---

## 🔧 核心修复内容

### ✅ 增加超时时间
```python
60 秒 → 180 秒（3 倍）
```

### ✅ 减少批次大小
```python
250-600 条 → 200-400 条（减少 20-33%）
```

### ✅ 减少并发线程
```python
4 线程 → 2 线程（减少 MongoDB 压力）
```

### ✅ 自动拆分超时批次
```python
超时时自动拆分为更小的批次重试
```

### ✅ 增加重试次数
```python
3 次 → 5 次
```

---

## 📊 预期效果

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| 超时频率 | 频繁 | < 5% |
| 成功率 | < 50% | > 95% |
| 单批时间 | 80-120s | 30-60s |

---

## 🚨 重要：添加 MongoDB 索引

**这是最有效的优化！**

```bash
# 1. 连接 MongoDB
mongo --host 10.70.223.31:27017 -u root -p

# 2. 选择数据库
use rqa;

# 3. 添加索引
db.paper_shards_detail_table_20230908.createIndex(
    { index: 1, doc_id: 1 },
    { background: true, name: "idx_index_docid" }
);

# 4. 验证索引
db.paper_shards_detail_table_20230908.getIndexes();
```

**效果**：查询速度提升 10-100 倍！

---

## 📝 如果问题仍存在

### 方案 A：更保守的配置

编辑 `search_srv_pipeline_v3_turbo.py`：

```python
# 找到这些行并修改：
MONGO_PARALLEL_WORKERS = 1  # 改为单线程（最稳定）
MONGO_MAX_TIME_MS = 300000  # 改为 5 分钟

# 在 get_optimal_batch_size 函数中：
def get_optimal_batch_size(total_conditions):
    if total_conditions < 1000:
        return 100  # 改为更小的批次
    elif total_conditions < 2000:
        return 150
    else:
        return 200
```

### 方案 B：分批延迟

添加批次间延迟，避免压垮 MongoDB：

```python
# 在 rank_pipeline 函数中，提交任务时添加延迟
import time

for i in range(0, total_conditions, BATCH_SIZE):
    batch_id = i // BATCH_SIZE + 1
    batch_conditions = search_conditions[i:i + BATCH_SIZE]
    
    future = executor.submit(...)
    future_to_batch[future] = batch_id
    
    # 添加延迟
    time.sleep(0.5)  # 每批延迟 0.5 秒
```

---

## 🎯 验证步骤

### 1. 查看批次执行情况
```bash
tail -f logs/search_9511.log | grep "MongoDB Batch"
```

**成功示例**：
```
[MongoDB Batch 1/10] 🔄 Querying 300 conditions...
[MongoDB Batch 1/10] ✅ 298 records in 35.23s
[MongoDB Batch 2/10] 🔄 Querying 300 conditions...
[MongoDB Batch 2/10] ✅ 295 records in 32.45s
```

**超时但会自动拆分**：
```
[MongoDB Batch 3/10] 🔄 Querying 350 conditions...
[MongoDB Batch 3/10] ⏰ TIMEOUT after 185.67s, reducing batch size and retrying...
[MongoDB Batch 3/10] 🔀 Splitting into 2 sub-batches: 175 + 175
[MongoDB Batch 3.1/10] 🔄 Querying 175 conditions...
[MongoDB Batch 3.1/10] ✅ 173 records in 65.23s
[MongoDB Batch 3.2/10] 🔄 Querying 175 conditions...
[MongoDB Batch 3.2/10] ✅ 172 records in 58.45s
```

### 2. 查看总体成功率
```bash
grep "PARALLEL query done" logs/search_9511.log | tail -10
```

**期望**：
```
[Rank] ✅ PARALLEL query done: success=10/10, failed=0
```

---

## 📞 联系支持

如果修复后问题仍然存在，请提供：

1. **日志文件**
   ```bash
   # 最近的 200 行日志
   tail -200 logs/search_9511.log > debug.log
   ```

2. **MongoDB 状态**
   ```bash
   mongo --host 10.70.223.31:27017 -u root -p
   db.serverStatus() > mongo_status.json
   ```

3. **配置信息**
   ```bash
   grep -E "MONGO_PARALLEL_WORKERS|MONGO_MAX_TIME_MS|get_optimal_batch_size" search_srv_pipeline_v3_turbo.py
   ```

---

## 🎉 修复完成检查清单

- [ ] 停止旧服务
- [ ] 使用修复后的代码
- [ ] 重启服务
- [ ] 查看日志验证
- [ ] **添加 MongoDB 索引**（最重要！）
- [ ] 监控成功率

完成后，系统应该可以稳定运行！🚀
