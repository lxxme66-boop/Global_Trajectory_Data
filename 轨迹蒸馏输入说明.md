# 轨迹蒸馏.py - 输入格式与API说明

## 问题1: 输入需要哪些字段？

### ✅ 必需字段

`qa_data.jsonl` 文件中每行需要包含以下字段：

```json
{
  "question_id": "qa_001",    // 必需：问题的唯一标识符（字符串或数字）
  "question": "问题内容？",    // 必需：用户的问题（字符串）
  "answer": "标准答案"         // 可选：用于记录ground truth，不影响推理过程
}
```

### 📝 字段说明

| 字段 | 类型 | 必需 | 说明 | 代码位置 |
|-----|------|------|------|---------|
| `question_id` | string/int | ✅ 必需 | 问题唯一标识，用于去重和断点续传 | 第414行 |
| `question` | string | ✅ 必需 | 用户的问题，作为推理的输入 | 第415行 |
| `answer` | string | ❌ 可选 | 标准答案，仅用于输出对比，不参与推理 | 第542行 |

### 🚨 验证逻辑

```python
# 代码第417-420行
if not question_id or not question:
    logging.warning(f"跳过缺少 'question_id' 或 'question' 的项")
    return True, {"question_id": question_id, "failure_reason": "Skipped: Missing QID or Question"}
```

**如果缺少 `question_id` 或 `question`，该条数据会被跳过**

### 📄 完整示例

```jsonl
{"question_id": "qa_001", "question": "什么是OLED显示技术？", "answer": "OLED是一种有机发光二极管显示技术"}
{"question_id": "qa_002", "question": "TFT-LCD的工作原理是什么？", "answer": "TFT-LCD利用薄膜晶体管控制液晶分子"}
{"question_id": "qa_003", "question": "MicroLED和MiniLED有什么区别？"}
```

> ⚠️ 注意：`answer` 字段可以省略，但建议保留用于后续评估

---

## 问题2: 火山API vs vLLM - 它们的作用是什么？

### 🧠 **vLLM** - 核心大模型推理引擎

**作用**: 这是整个系统的**大脑**，负责所有的推理决策

#### 使用场景1: Judge阶段（判断知识缺口）
```python
# 代码第434行
response_text = call_vllm_api(SYSTEM_PROMPT_JUDGE, user_prompt_judge, temperature=0.7)
```

**输入示例**:
```
[问题]：什么是OLED显示技术?
[已检索的上下文]：无

[你的任务]：判断知识是否足够，如果不够则生成搜索查询
```

**vLLM输出**:
```json
{
  "knowledge_gap": true,
  "reasoning": "用户询问OLED技术，我需要检索相关信息",
  "query": "OLED显示技术的基本原理是什么？"
}
```

#### 使用场景2: Answer阶段（生成最终答案）
```python
# 代码第531行
final_answer = call_vllm_api(SYSTEM_PROMPT_ANSWERER, user_prompt_ans, temperature=0.7)
```

**输入示例**:
```
问题: 什么是OLED显示技术?
上下文: [检索到的知识块1]: OLED是有机发光二极管...
       [检索到的知识块2]: OLED不需要背光源...
```

**vLLM输出**:
```
OLED(有机发光二极管)是一种显示技术，它使用有机材料在电流作用下发光...
```

#### 配置信息
```python
CONFIG = {
    "VLLM_API_ENDPOINT": "http://127.0.0.1:8000/v1/chat/completions",
    "VLLM_MODEL_NAME": "/path/to/qwen3-32b-lora",  # 微调后的Qwen3-32B模型
    "VLLM_API_KEY": "EMPTY"
}
```

---

### 🌐 **火山引擎API** - 辅助翻译服务

**作用**: 仅用于**中英文翻译**，提升RAG检索的召回率

#### 使用场景: 双语混合检索

```
用户问题: "什么是OLED显示技术？"  (中文)
    ↓
火山API检测语种 → 返回 "zh"
    ↓
火山API翻译 → "What is OLED display technology?"  (英文)
    ↓
并行检索:
  - 原文(中文): "什么是OLED显示技术？" → RAG检索
  - 译文(英文): "What is OLED..." → RAG检索
    ↓
合并去重 → 返回更全面的知识块
```

#### 代码位置
```python
# 语种检测 (第288行)
lang = _detect_language(query)  # 调用火山API

# 文本翻译 (第295-300行)
if lang == 'zh':
    translated_query = _translate_text(original_query, "en")  # 中→英
elif lang == 'en':
    translated_query = _translate_text(original_query, "zh")  # 英→中
```

#### 配置信息
```python
AK = "AKLTOWZhOWQ1ZjAzZTFkNDc2NjhmNTJjMmVkYmQ2NmIzODA"
SK = "TnpsaE5tWTBaV1V4WkRCak5EZGhOVGxsTldOa09UQTNaRGMyWm1JNVlqQA=="
```

#### ⚠️ 火山API可以禁用
如果火山API不可用，脚本会降级为**单语检索**（只用原文搜索）

---

## 🔄 完整推理流程

```
┌─────────────────────────────────────────────────────┐
│  输入: qa_data.jsonl                                │
│  {"question_id": "qa_001", "question": "问题?"}     │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│  Hop 1: vLLM Judge判断                              │
│  → 输入: 问题 + 当前上下文(初始为"无")               │
│  → 输出: {knowledge_gap: true, query: "搜索查询"}   │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│  RAG检索 (带双语增强)                                │
│  1. 火山API检测语种: zh/en                           │
│  2. 火山API翻译: 中→英 or 英→中                      │
│  3. 并行调用RAG检索 (原文 + 译文)                    │
│  4. 合并去重知识块                                   │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│  累积上下文，重复Hop 2, 3, 4, 5...                   │
│  (直到vLLM Judge判断knowledge_gap=false)             │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│  最终答案: vLLM Answerer生成                         │
│  → 输入: 问题 + 所有累积的上下文                      │
│  → 输出: 完整答案                                    │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│  输出: inference_trajectory.jsonl                    │
│  包含完整的推理轨迹(每一跳的query和retrieved_chunks)  │
└─────────────────────────────────────────────────────┘
```

---

## 📊 API调用次数统计

对于一个典型的2跳推理任务：

| API服务 | 调用次数 | 作用 |
|---------|---------|------|
| **vLLM** | **3次** | 2次Judge + 1次Answer |
| **火山翻译** | 2次 | 每跳1次翻译 |
| **火山语种检测** | 2次 | 每跳1次检测 |
| **RAG检索** | 4次 | 每跳2次(原文+译文) |

> 💡 **vLLM是调用最频繁、最核心的服务**

---

## 🚀 启动命令对比

### vLLM (必需)
```bash
# 启动vLLM服务
vllm serve /path/to/qwen3-32b-lora \
  --host 127.0.0.1 \
  --port 8000 \
  --api-key EMPTY
```

### 火山API (可选)
```bash
# 无需启动服务，只需环境变量
export VOLCENGINE_ACCESS_KEY="AKLTOWZh..."
export VOLCENGINE_SECRET_KEY="TnpsaE5tWT..."
```

---

## ✅ 总结

| 问题 | 答案 |
|-----|------|
| **输入必需字段** | `question_id` (必需), `question` (必需), `answer` (可选) |
| **vLLM的作用** | 🧠 **核心推理引擎** - 判断知识缺口 + 生成最终答案 |
| **火山API的作用** | 🌐 **辅助翻译服务** - 中英互译，提升RAG召回率(可禁用) |
| **谁更重要？** | **vLLM是核心**，火山API只是锦上添花 |

---

## 🔍 代码验证

想验证输入格式？可以运行：
```bash
# 创建测试输入
cat > /tmp/test_input.jsonl << 'EOF'
{"question_id": "test_001", "question": "测试问题"}
{"question_id": "test_002"}
{"question": "缺少question_id的问题"}
EOF

# 修改脚本的INPUT_FILE指向测试文件
# 运行后会看到：
# ✅ test_001 - 正常处理
# ⚠️ test_002 - 被跳过(缺少question)
# ⚠️ 第3行 - 被跳过(缺少question_id)
```
